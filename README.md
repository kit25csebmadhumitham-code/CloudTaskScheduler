# CloudTaskScheduler
A Java-based Cloud Task Scheduling System that efficiently manages and executes tasks using a Priority Queue (Max Heap). The system dynamically schedules tasks based on priority and enables fast searching using Binary Search, simulating real-world cloud and CPU scheduling mechanisms.
